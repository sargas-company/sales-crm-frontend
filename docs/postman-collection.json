{
  "info": {
    "name": "Sales CRM API",
    "description": "# Sales CRM API Collection\n\n## Getting Started\n\n1. **Register** — create an account (one time)\n2. **Login** — token is saved automatically to `{{token}}` variable\n3. **Get Platforms** — copy an ID to `{{platformId}}`\n4. **Create Account** — copy the returned ID to `{{accountId}}`\n5. **Create Proposal** — uses `{{accountId}}` and `{{platformId}}`\n\n## Base URL\nChange `{{baseUrl}}` variable if needed (default: `http://localhost:3000`)\n\n## Enums\n\n**proposalType:** `Bid` | `Invite` | `DirectMessage`\n\n**status:** `Draft` | `Sent` | `Viewed` | `Replied`\n\n## Notes\n- `id`, `createdAt`, `updatedAt` are auto-generated by the server\n- `sentAt` is set automatically when `status` changes to `Sent`\n- `boosted`, `connects`, `boostedConnects` only apply when `proposalType` is `Bid`\n- `boostedConnects` is reset to `0` if `boosted` is `false`\n- Pagination: `GET /proposals?page=1&limit=10` returns `{ data: [...], total: N }`",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:3000",
      "description": "Base API URL."
    },
    {
      "key": "token",
      "value": "",
      "description": "JWT access token. Set automatically after Login."
    },
    {
      "key": "platformId",
      "value": "00000000-0000-0000-0000-000000000001",
      "description": "Platform ID. Default is Upwork. Get full list from GET /platforms."
    },
    {
      "key": "accountId",
      "value": "",
      "description": "Account ID. Set automatically after Create Account."
    },
    {
      "key": "proposalId",
      "value": "",
      "description": "Proposal ID. Set automatically after Create Proposal."
    }
  ],
  "item": [
    {
      "name": "Auth",
      "description": "Authentication endpoints. Run Register once, then Login — token saves automatically.",
      "item": [
        {
          "name": "Register",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/auth/register",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"test@example.com\",\n  \"password\": \"password123\"\n}"
            },
            "description": "Create a new user account. Only needed once."
          }
        },
        {
          "name": "Login",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const json = pm.response.json();",
                  "if (json.accessToken) {",
                  "  pm.collectionVariables.set('token', json.accessToken);",
                  "  console.log('Token saved successfully');",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/auth/login",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"admin@test.com\",\n  \"password\": \"admin123\"\n}"
            },
            "description": "Login and get JWT token. Token is saved automatically to {{token}} variable."
          }
        }
      ]
    },
    {
      "name": "Platforms",
      "description": "Platform management. Platforms are global (shared across all users). Default platforms — Upwork and LinkedIn — are seeded on first deploy.",
      "item": [
        {
          "name": "Get All Platforms",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const json = pm.response.json();",
                  "if (Array.isArray(json) && json.length > 0) {",
                  "  const upwork = json.find(p => p.slug === 'upwork') || json[0];",
                  "  pm.collectionVariables.set('platformId', upwork.id);",
                  "  console.log('Platform ID set to:', upwork.id, '(' + upwork.title + ')');",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [{ "key": "Authorization", "value": "Bearer {{token}}" }],
            "url": "{{baseUrl}}/platforms",
            "description": "Get all platforms ordered by title.\n\nUpwork ID is saved automatically to `{{platformId}}`.\n\n**Seeded defaults:**\n- `00000000-0000-0000-0000-000000000001` — Upwork\n- `00000000-0000-0000-0000-000000000002` — LinkedIn"
          }
        },
        {
          "name": "Get Platform by ID",
          "request": {
            "method": "GET",
            "header": [{ "key": "Authorization", "value": "Bearer {{token}}" }],
            "url": "{{baseUrl}}/platforms/{{platformId}}",
            "description": "Get a single platform by ID."
          }
        },
        {
          "name": "Create Platform",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{token}}" }
            ],
            "url": "{{baseUrl}}/platforms",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"New Platform\",\n  \"slug\": \"new-platform\",\n  \"imageUrl\": \"https://example.com/logo.png\"\n}"
            },
            "description": "Create a new platform.\n\n**Required:** `title`, `slug`\n**Optional:** `imageUrl`\n\n`slug` must be unique. Returns `409` if already taken."
          }
        },
        {
          "name": "Update Platform",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{token}}" }
            ],
            "url": "{{baseUrl}}/platforms/{{platformId}}",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"imageUrl\": \"https://example.com/new-logo.png\"\n}"
            },
            "description": "Update platform fields. All fields optional.\n\nUpdating `imageUrl` here automatically affects all accounts linked to this platform."
          }
        },
        {
          "name": "Delete Platform",
          "request": {
            "method": "DELETE",
            "header": [{ "key": "Authorization", "value": "Bearer {{token}}" }],
            "url": "{{baseUrl}}/platforms/{{platformId}}",
            "description": "Delete a platform. Returns 204 No Content.\n\n**Warning:** Cannot delete if accounts are linked to it."
          }
        }
      ]
    },
    {
      "name": "Accounts",
      "description": "Developer account management. Each account represents a developer profile on a specific platform (e.g. Dmytro on Upwork). Scoped to the authenticated user.",
      "item": [
        {
          "name": "Get All Accounts",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const json = pm.response.json();",
                  "if (Array.isArray(json) && json.length > 0) {",
                  "  pm.collectionVariables.set('accountId', json[0].id);",
                  "  console.log('Account ID set to:', json[0].id, '(' + json[0].firstName + ' ' + json[0].lastName + ')');",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [{ "key": "Authorization", "value": "Bearer {{token}}" }],
            "url": "{{baseUrl}}/accounts",
            "description": "Get all accounts for the authenticated user, ordered by lastName then firstName.\n\nFirst account ID is saved automatically to `{{accountId}}`.\n\nEach account includes nested `platform` object with `imageUrl`."
          }
        },
        {
          "name": "Get Account by ID",
          "request": {
            "method": "GET",
            "header": [{ "key": "Authorization", "value": "Bearer {{token}}" }],
            "url": "{{baseUrl}}/accounts/{{accountId}}",
            "description": "Get a single account by ID."
          }
        },
        {
          "name": "Create Account",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const json = pm.response.json();",
                  "if (json.id) {",
                  "  pm.collectionVariables.set('accountId', json.id);",
                  "  console.log('Account ID saved:', json.id);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{token}}" }
            ],
            "url": "{{baseUrl}}/accounts",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"firstName\": \"Dmytro\",\n  \"lastName\": \"Sarafaniuk\",\n  \"platformId\": \"{{platformId}}\"\n}"
            },
            "description": "Create a new account.\n\n**Required:** `firstName`, `lastName`, `platformId`\n\nOne account per platform per user. Returns `409` if account for this platform already exists.\n\nGet `platformId` from `GET /platforms`.\n\nAccount ID is saved automatically to `{{accountId}}`."
          }
        },
        {
          "name": "Update Account",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{token}}" }
            ],
            "url": "{{baseUrl}}/accounts/{{accountId}}",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"firstName\": \"Dmytro\",\n  \"lastName\": \"S.\"\n}"
            },
            "description": "Update account fields. All fields optional."
          }
        },
        {
          "name": "Delete Account",
          "request": {
            "method": "DELETE",
            "header": [{ "key": "Authorization", "value": "Bearer {{token}}" }],
            "url": "{{baseUrl}}/accounts/{{accountId}}",
            "description": "Delete an account. Returns 204 No Content."
          }
        }
      ]
    },
    {
      "name": "Proposals",
      "description": "Proposal CRUD. Requires accountId and platformId — get them from /accounts and /platforms first.",
      "item": [
        {
          "name": "Create Proposal",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "const json = pm.response.json();",
                  "if (json.id) {",
                  "  pm.collectionVariables.set('proposalId', json.id);",
                  "  console.log('Proposal ID saved:', json.id);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{token}}" }
            ],
            "url": "{{baseUrl}}/proposals",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Full Stack Developer — MVP Project\",\n  \"accountId\": \"{{accountId}}\",\n  \"platformId\": \"{{platformId}}\",\n  \"proposalType\": \"Bid\",\n  \"jobUrl\": \"~01abc1234567890def\",\n  \"boosted\": true,\n  \"connects\": 6,\n  \"boostedConnects\": 14,\n  \"coverLetter\": \"Dear client, we are excited to work on your project...\",\n  \"vacancy\": \"We are looking for a React developer with 3+ years of experience...\"\n}"
            },
            "description": "Create a new proposal.\n\n**Required:** `title`, `accountId`, `platformId`, `proposalType`\n\n**Optional:** `jobUrl`, `boosted`, `connects`, `boostedConnects`, `coverLetter`, `vacancy`\n\n**proposalType:** `Bid` | `Invite` | `DirectMessage`\n\n**Connects rules:**\n- `boosted`, `connects`, `boostedConnects` only apply for `Bid`\n- If `boosted: false` → `boostedConnects` is forced to `0`\n- If `proposalType` is not `Bid` → all three are forced to `false`/`0`/`0`\n\nProposal ID is saved automatically to `{{proposalId}}`."
          }
        },
        {
          "name": "Get All Proposals (paginated)",
          "request": {
            "method": "GET",
            "header": [{ "key": "Authorization", "value": "Bearer {{token}}" }],
            "url": {
              "raw": "{{baseUrl}}/proposals?page=1&limit=10",
              "host": ["{{baseUrl}}"],
              "path": ["proposals"],
              "query": [
                { "key": "page", "value": "1", "description": "Page number (default: 1)" },
                { "key": "limit", "value": "10", "description": "Items per page (default: 10)" }
              ]
            },
            "description": "Get paginated list of proposals for the authenticated user. Ordered by `createdAt` descending.\n\n**Response:**\n```json\n{ \"data\": [...], \"total\": 42 }\n```\n\nEach proposal includes nested `user`, `account` (with `platform`), and `platform` objects."
          }
        },
        {
          "name": "Get Proposal by ID",
          "request": {
            "method": "GET",
            "header": [{ "key": "Authorization", "value": "Bearer {{token}}" }],
            "url": "{{baseUrl}}/proposals/{{proposalId}}",
            "description": "Get a single proposal by ID. Includes `user`, `account.platform`, and `platform` objects."
          }
        },
        {
          "name": "Update Proposal",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{token}}" }
            ],
            "url": "{{baseUrl}}/proposals/{{proposalId}}",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"Sent\",\n  \"coverLetter\": \"Updated cover letter text...\",\n  \"boosted\": true,\n  \"connects\": 8,\n  \"boostedConnects\": 14\n}"
            },
            "description": "Update any proposal fields. All fields are optional — send only what needs to change.\n\n**status:** `Draft` | `Sent` | `Viewed` | `Replied`\n\nWhen `status` changes to `Sent`, `sentAt` is set automatically by the server."
          }
        },
        {
          "name": "Delete Proposal",
          "request": {
            "method": "DELETE",
            "header": [{ "key": "Authorization", "value": "Bearer {{token}}" }],
            "url": "{{baseUrl}}/proposals/{{proposalId}}",
            "description": "Delete a proposal by ID. Returns 204 No Content."
          }
        },
        {
          "name": "Get Chat History",
          "request": {
            "method": "GET",
            "header": [{ "key": "Authorization", "value": "Bearer {{token}}" }],
            "url": "{{baseUrl}}/proposals/{{proposalId}}/chat",
            "description": "Get all chat messages for a proposal, ordered by `createdAt` ascending.\n\n**Response:**\n```json\n[\n  { \"id\": \"...\", \"role\": \"user\", \"content\": \"...\", \"createdAt\": \"...\" },\n  { \"id\": \"...\", \"role\": \"assistant\", \"content\": \"...\", \"decision\": \"bid\", \"reasoning\": \"...\", \"createdAt\": \"...\" }\n]\n```"
          }
        },
        {
          "name": "Analyze Proposal Intent (AI)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{token}}" }
            ],
            "url": "{{baseUrl}}/proposals/{{proposalId}}/analyze",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"content\": \"Should we bid on this job? The client has good reviews.\"\n}"
            },
            "description": "Analyze a message against the proposal context using AI — without generating a full chat response.\n\n**Response:**\n```json\n{ \"decision\": \"bid\", \"reasoning\": \"...\" }\n```"
          }
        }
      ]
    }
  ]
}
